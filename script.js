/* try {

    console.log(a);

} catch (e) {

    console.log('error');
}

 */

try {

    const arr = new Array();
    let size = prompt('enter size')
    arr.length = size;

} catch (error) {
    console.log('addade sahih vared konid');
}